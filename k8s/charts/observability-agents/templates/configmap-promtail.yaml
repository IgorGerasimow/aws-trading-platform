apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "observability-agents.fullname" . }}-promtail
  labels:
    {{- include "observability-agents.labels" . | nindent 4 }}
data:
  promtail.yaml: |
    {{- .Values.promtail.config | nindent 4 }}
